# PostgreSQL（生产/验收推荐）
# DATABASE_URL=postgresql+psycopg2://editor:editor@localhost:5432/photo_merge
# 留空则使用本地 SQLite，便于离线试跑
DATABASE_URL=
STORAGE_DIR=./storage
# pHash 汉明距离阈值（0~64，越小越严格；16 可保留“同场景不同照片”的边缘候选供人工识别）
PHASH_THRESHOLD=16
MAX_DIMENSION=2000
JPEG_QUALITY=85
THUMB_SIZE=320
